<%= form_for(@program, html: { multipart: true, id: 'program1' }, remote: true) do |f| %>
    <% if @program.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(program.errors.count, "error") %> prohibited this program from being saved:</h2>

          <ul>
            <% @program.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="row">
      <div class="col-sm-1"></div>
      <div class="col-sm-8">
        <div class="field">
          <h5 class="uppercase mb8"><b>Nombre</b></h5>
          <%= f.text_field :name %>
        </div>

        <div class="field">
          <h5 class="uppercase mb8"><b>Descripción</b></h5>
          <%= f.text_area :description %>
        </div>

        <div class="control-group">
          <h5 class="uppercase mb8"><b>Archivos</b>
            <div class="controls">
              <%= file_field_tag 'sounds[]', type: :file, multiple: true if @program.new_record?%>
            </div>
          </h5>
        </div>
      </div>
      <div class="col-sm-3"></div>
    </div>
    <br>

    <div class="row">
      <div class="col-sm-1"></div>
      <div class="col-sm-2">
        <div class="actions">
          <% if @program.new_record? %>
              <%= f.submit :type => 'submit', :value => 'Subir programa' %>
          <% else %>
              <%= f.submit :type => 'submit', :value => 'Actualizar programa' %>
          <% end %>
        </div>
      </div>
      <div class="col-sm-9"></div>
    </div>
    <br>
<% end %>
